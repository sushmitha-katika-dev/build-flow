package com.buildflow.gateway.security;

import org.springframework.stereotype.Component;

@Component
public class JwtTokenProvider {
    // Utility for token generation, normally used in auth-service, but included in gateway structure
}
