package com.buildflow.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
    // General Gateway configurations if not in application.yml
}
