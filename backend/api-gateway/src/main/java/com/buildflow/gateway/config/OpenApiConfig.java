package com.buildflow.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Configuration for swagger integration across microservices
}
